# STATE.md — ⚠️ ARCHIVE, not live

**This repo is a stale copy. The live analysis-VPS repo is `lironzruya7/analysis-vps`.**
Do not develop here, and do not trust this file as deployment state — see the archive
note at the top of `CLAUDE.md` (established 2026-07-27 by comparing the two clones).

- **Live deploy state** is in `analysis-vps` → `docs/memory/STATE.md`.
- The 77-line "live deployment state" this file used to carry described `analysis-vps`
  as of `main @ d5b23cb`, and was **already stale when this repo was frozen** — the live
  repo is many commits ahead (toolbox expansion, supply-chain pins, the egress ip-rule
  fix). The frozen text is preserved in this repo's git history if ever needed.
- Nothing is deployed *from* this repo. There is nothing to roll back here.

_Reduced to an archive marker 2026-07-30, so a reader can never mistake a frozen copy
for live state. The owner decides if/when `lironzruya7/Projects` is deleted on GitHub._
